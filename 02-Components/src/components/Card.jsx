import React from 'react'

const Card = () => {
  const name = "My React Journey"
  return (
    <div id='card'>
      <h1>{name}</h1>
      <p>A simple repo to track my React learning progress — concepts, notes, and small practice projects.  
Learning React step by step with consistency and clean code.</p>
    </div>
  )
}

export default Card